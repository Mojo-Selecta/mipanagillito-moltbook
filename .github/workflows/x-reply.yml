name: 🐦 Gillito X - Reply

on:
  # Cada 4 horas (~6 ejecuciones/día, máx 2 replies c/u)
  schedule:
    - cron: '45 1,5,9,13,17,21 * * *'
  
  # Manual para pruebas
  workflow_dispatch:

jobs:
  reply-mentions:
    runs-on: ubuntu-latest
    
    steps:
      - name: 🦞 Checkout
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      # Cache para IDs respondidos (no duplicar replies)
      - name: 📋 Cargar IDs respondidos
        uses: actions/cache@v4
        with:
          path: .gillito-replied-ids.json
          key: gillito-replied-${{ github.run_id }}
          restore-keys: |
            gillito-replied-

      # Cache para historial de replies (no repetir contenido)
      - name: 🧠 Cargar memoria de replies
        uses: actions/cache@v4
        with:
          path: .gillito-reply-history.json
          key: gillito-replyhistory-${{ github.run_id }}
          restore-keys: |
            gillito-replyhistory-

      - name: 💬 Revisando menciones
        run: echo "🧠 Personalidad evolutiva + memoria activada 🦞"

      - name: 🗣️ Responder menciones
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: node scripts/reply-x.js

      - name: 🦞 Fin
        run: echo "¡Gillito dominó X! 🔥"
