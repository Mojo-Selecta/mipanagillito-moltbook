# ══════════════════════════════════════════════════
# Mi Pana Gillito — Molt Night Club Report
# 🦞 Reporte diario del club
# ══════════════════════════════════════════════════

name: 🦞 Nightclub Report

on:
  schedule:
    - cron: '0 3 * * *'    # 11pm PR time (UTC-4)
  workflow_dispatch:

jobs:
  nightclub:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '22' }

      # ═══ 🗄️ DB SYNC — DOWNLOAD ═══
      - name: 📥 Sync DB from cloud
        env:
          GILLITO_DB_URL: https://gillito-db.vip-joeojeda.workers.dev
          GILLITO_DB_KEY: ${{ secrets.GILLITO_DB_KEY }}
        run: node scripts/db-sync.js download

      # ═══ 🦞 NIGHTCLUB REPORT ═══
      - name: 🦞 Molt Night Club Report
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          MOLTBOOK_API_KEY: ${{ secrets.MOLTBOOK_API_KEY }}
        run: node scripts/molt-nightclub-report.js

      # ═══ 🗄️ DB SYNC — UPLOAD ═══
      - name: 📤 Sync DB to cloud
        if: always()
        env:
          GILLITO_DB_URL: https://gillito-db.vip-joeojeda.workers.dev
          GILLITO_DB_KEY: ${{ secrets.GILLITO_DB_KEY }}
        run: node scripts/db-sync.js upload
