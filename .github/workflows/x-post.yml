name: 🐦 Gillito X - Post

on:
  # Cada 3 horas (~8 posts/día)
  schedule:
    - cron: '15 */3 * * *'
  
  # Manual para pruebas
  workflow_dispatch:

jobs:
  post-to-x:
    runs-on: ubuntu-latest
    
    steps:
      - name: 🦞 Checkout
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      # Cache para recordar tweets anteriores (no repetir)
      - name: 🧠 Cargar memoria de tweets
        uses: actions/cache@v4
        with:
          path: .gillito-tweet-history.json
          key: gillito-history-${{ github.run_id }}
          restore-keys: |
            gillito-history-

      - name: 🔥 Gillito va a X
        run: echo "¡LLEGUÉ PUÑETA! 🧠 Personalidad evolutiva activada 🦞"

      - name: 📝 Postear a X
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
        run: node scripts/post-to-x.js

      - name: 🦞 Fin
        run: echo "¡Gillito dominó X! 🔥"
